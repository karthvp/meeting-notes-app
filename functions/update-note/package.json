{
  "name": "egen-meeting-notes-update-note",
  "version": "1.0.0",
  "description": "Update note Cloud Function for Egen Meeting Notes",
  "main": "index.js",
  "scripts": {
    "start": "npx @google-cloud/functions-framework --target=updateNote",
    "dev": "npx @google-cloud/functions-framework --target=updateNote --signature-type=http",
    "deploy": "gcloud functions deploy update-note --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point updateNote --source=. --project=karthik-patil-sandbox --region=us-central1 --gen2",
    "deploy:prod": "gcloud functions deploy update-note --runtime nodejs20 --trigger-http --entry-point updateNote --source=. --project=karthik-patil-sandbox --region=us-central1 --gen2 --no-allow-unauthenticated"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.3.0",
    "@google-cloud/functions-framework": "^3.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
