{
  "name": "egen-meeting-notes",
  "version": "1.0.0",
  "description": "A custom Granola.ai alternative built on Google Workspace for organizing meeting notes",
  "main": "index.js",
  "scripts": {
    "test": "jest --config jest.config.js",
    "test:watch": "jest --watch",
    "test:integration": "jest --config jest.integration.config.js",
    "setup:gcp": "cd infrastructure/gcp && ./setup-project.sh",
    "setup:firestore": "cd infrastructure/firestore && npm install && npm run seed",
    "setup:drive": "cd infrastructure/drive && npm install && npm run setup",
    "setup:all": "npm run setup:gcp && npm run setup:firestore && npm run setup:drive",
    "dev": "cd functions && npm run dev",
    "deploy": "cd functions && npm run deploy",
    "deploy:prod": "cd functions && npm run deploy:prod"
  },
  "keywords": [
    "meeting-notes",
    "google-workspace",
    "google-drive",
    "firestore",
    "cloud-functions"
  ],
  "author": "Egen",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
