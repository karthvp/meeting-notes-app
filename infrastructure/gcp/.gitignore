# GCP Infrastructure - Git Ignore

# Service account keys - NEVER commit these
service-account-key.json
*.json.key
*-credentials.json

# OAuth client secrets
oauth-*.json
client_secret*.json

# Environment files with secrets
.env
.env.local
.env.*.local

# Terraform state (if using Terraform later)
*.tfstate
*.tfstate.*
.terraform/
